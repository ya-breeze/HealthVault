backend/bin/
*.db
*.db-shm
*.db-wal
node_modules/
frontend/.next/
frontend/out/
.env
test-results/

# Playwright run artifacts. e2e/.gitignore already covers e2e/test-results/,
# but Playwright writes them relative to the cwd it was started from, so a run
# launched from the repository root lands here instead.
test-results/
playwright-report/

# ralphex agent state
.ralphex/
