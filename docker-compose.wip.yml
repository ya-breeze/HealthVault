services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      HCW_PORT: "8080"
      HCW_DBPATH: /data/hcw.db
      HCW_SEED_USERS: ${HCW_SEED_USERS:-TestFamily:alice:pass1}
      HCW_JWT_SECRET: ${HCW_JWT_SECRET:-hcw-wip-secret}
      HCW_COOKIE_SECURE: "false"
      HCW_MCP_TOKEN: ${HCW_MCP_TOKEN}
      HCW_UPLOADS_DIR: ${HCW_UPLOADS_DIR:-/data/uploads}
      HCW_USDA_DB_PATH: ${HCW_USDA_DB_PATH:-/data/usda.db}
      HCW_OPENAI_API_KEY: ${HCW_OPENAI_API_KEY:-}
      HCW_OPENAI_MODEL: ${HCW_OPENAI_MODEL:-gpt-4o-mini}
    volumes:
      - /mnt/eight-2/eight-2/data/data/hcw-wip:/data
    networks:
      - hcw-network

  nginx:
    build:
      context: .
      dockerfile: ./nginx/Dockerfile
    restart: unless-stopped
    ports:
      - "8888:80"
      - "9888:443"
    depends_on:
      - backend
    networks:
      - hcw-network

networks:
  hcw-network:
